{
  "parameters": {
    "environmentName": { "value": "${AZURE_ENV_NAME}" },
    "location": { "value": "${AZURE_LOCATION}" },
    "githubToken": { "value": "${GITHUB_TOKEN}" },
    "useAzureModel": { "value": "${USE_AZURE_MODEL=false}" },
    "azureModelName": { "value": "${AZURE_MODEL_NAME=o4-mini}" },
    "azureModelVersion": { "value": "${AZURE_MODEL_VERSION=2025-04-16}" },
    "azureModelCapacity": { "value": "${AZURE_MODEL_CAPACITY=100}" },
    "foundryProjectEndpoint": { "value": "${FOUNDRY_PROJECT_ENDPOINT=}" },
    "agentInventoryName": { "value": "${AGENT_INVENTORY_NAME=}" },
    "agentForecastName": { "value": "${AGENT_FORECAST_NAME=}" },
    "agentComplianceName": { "value": "${AGENT_COMPLIANCE_NAME=}" },
    "resourceGroupName": { "value": "${AZURE_RESOURCE_GROUP=rg-${AZURE_ENV_NAME}}" }
  }
}
